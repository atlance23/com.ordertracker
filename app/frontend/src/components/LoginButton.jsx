export default function LoginButton() {

    return (
            <>
                <button onClick={() => loginWithRedirect()}>
                    Sign In
                </button>
            </>
    )
}